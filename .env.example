# AgentPay environment variables
# Copy this file to .env.local and fill in values

# Public base URL — used by the MCP server to build confirmation URLs
# Defaults to https://agentpay-tan.vercel.app if unset (production Vercel URL).
# Override for local dev: NEXT_PUBLIC_BASE_URL=http://localhost:3000
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# StraitsX MCP endpoints (Dev Hub provided) — used from phase 3
# STRAITSX_SANDBOX_MCP_URL=https://card.straitsx.ai/sandbox/sse
# STRAITSX_PRODUCTION_MCP_URL=https://card.straitsx.ai/production/sse
# STRAITSX_MCP_PASSPHRASE=  # per-team, from Dev Hub

# Avalanche RPC (public) — used from phase 3
# AVALANCHE_MAINNET_RPC=https://api.avax.network/ext/bc/C/rpc
# AVALANCHE_FUJI_RPC=https://api.avax-test.network/ext/bc/C/rpc

# Server wallet (phase 3c) — pays the x402 mint price in XSGD and signs Block
# Receipts. Self-custody wallet, funded for real spend in production mode.
# Used by src/lib/straitsx/client.ts and scripts/mint-test.ts.
# Copy this file to .env and fill in the real value. NEVER commit the real .env. NEVER paste this key into any chat.
# WALLET_PRIVATE_KEY=0x-prefixed, 64 hex chars

# Owner of the Mint Gate (phase 3c) — execute_purchase refuses any
# confirmation whose recovered signer is not this address. Unset = the gate
# fails closed and refuses everything.
# OWNER_ADDRESS=0x-prefixed wallet address

# StraitsX environment for real mints: "production" (default) or "sandbox".
# STRAITSX_ENV=production
