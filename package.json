{
  "name": "agentpay",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AgentPay — the trust layer that lets AI agents spend safely. StraitsX AgentiX Playground, Track 2.",
  "scripts": {
    "mint:sandbox": "tsx scripts/mint-test.ts sandbox",
    "mint:production": "tsx scripts/mint-test.ts production"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
